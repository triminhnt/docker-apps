# Extensions 
Docker v1.23.1

nginx 

% docker build . -t img-html:v1

% docker images

# Create Container
% docker run -d -p 80:80 --name cons-html img-html:v1

# Create Container
docker run -d -p 80:80 --name cons-docker docker/getting-started:latest

# Truy cap vao docker container
docker exec -it cons-docker bash

# Tao ban sao image
docker tag docker/getting-started:latest new-img:v1

docker tag new-img:v1 jimnguyen2/nodejs25:v1

# Dua image len docker hub
docker push jimnguyen2/nodejs25:v1





